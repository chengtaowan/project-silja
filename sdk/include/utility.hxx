#pragma once

namespace uti {
   [[ nodiscard ]]
   std::addr_t lookup_process_by_name(
      const wchar_t* process_name
   ) {
      // get initial process
      // get mmprocesslinks
      // walk
      // get full image name
      // wcsstr
      // ret
   }
}
